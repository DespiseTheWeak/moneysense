
/*bool strealise = true;

static int coolVar = 0;
while (true) {
	coolVar++;
	if (coolVar == 4) coolVar = 0;
	Sleep(500);
}

if (strealise) {
	{
		switch (coolVar) {
		case 0:
			render::get().text(fonts[BIGVERMIN], screen_width / 2.f, screen_height / 2.f, Color(255, 255, 255, 200), HFONT_CENTERED_NONE, "SECTUM.CC");
			break;
		case 1:
			render::get().text(fonts[BIGVERMIN], screen_width / 2.f, screen_height / 2.f, Color(255, 255, 255, 200), HFONT_CENTERED_NONE, "#ECTUM.CC");
			break;
		case 2:
			render::get().text(fonts[BIGVERMIN], screen_width / 2.f, screen_height / 2.f, Color(255, 255, 255, 200), HFONT_CENTERED_NONE, "S#CTUM.CC");
			break;
		case 3:
			render::get().text(fonts[BIGVERMIN], screen_width / 2.f, screen_height / 2.f, Color(255, 255, 255, 200), HFONT_CENTERED_NONE, "SE#TUM.CC");
			break;
		case 4:
			render::get().text(fonts[BIGVERMIN], screen_width / 2.f, screen_height / 2.f, Color(255, 255, 255, 200), HFONT_CENTERED_NONE, "SEC#UM.CC");
			break;
		case 5:
			render::get().text(fonts[BIGVERMIN], screen_width / 2.f, screen_height / 2.f, Color(255, 255, 255, 200), HFONT_CENTERED_NONE, "SECT#M.CC");
			break;
		case 6:
			render::get().text(fonts[BIGVERMIN], screen_width / 2.f, screen_height / 2.f, Color(255, 255, 255, 200), HFONT_CENTERED_NONE, "SECTU#.CC");
			break;
		case 7:
			render::get().text(fonts[BIGVERMIN], screen_width / 2.f, screen_height / 2.f, Color(255, 255, 255, 200), HFONT_CENTERED_NONE, "SECTUM#CC");
			break;
		case 8:
			render::get().text(fonts[BIGVERMIN], screen_width / 2.f, screen_height / 2.f, Color(255, 255, 255, 200), HFONT_CENTERED_NONE, "SECTUM.#C");
			break;
		case 9:
			render::get().text(fonts[BIGVERMIN], screen_width / 2.f, screen_height / 2.f, Color(255, 255, 255, 200), HFONT_CENTERED_NONE, "SECTUM.C#");
			break;
		}
	}
}*/